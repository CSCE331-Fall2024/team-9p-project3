export default function OrderingTopPanel({ title }) {
    return (
        <div className="flex justify-center items-center h-1/6 w-full bg-red-200">
        <h1 className="text-black text-5xl">{title}</h1>
        </div>
    );
}